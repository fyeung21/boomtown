const styles = theme => ({
  background: {
    backgroundColor: "#262626"
  },
  itemGrid: {
    paddingTop: 100,
    display: "grid",
    gridTemplateColumns: "repeat(3, 1fr)",
    gridGap: 80,
    margin: '0 3rem'
  }
});

export default styles;
